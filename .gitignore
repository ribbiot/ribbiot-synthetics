# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info

# Environment and secrets - never commit actual credentials
*.tfvars
!*.tfvars.example
.env
.env.*
!.env.example

# Node
node_modules/
dist/
*.tsbuildinfo

# IDE and OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Plan output (optional; some teams commit for review)
# *.plan

# MCP: per-developer schema selection (run npm run mcp:select-schema to create)
config/selected-schema.json
